{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile.scraper"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "startCommand": "python -u ingestion/govbr_keepalive.py --once",
    "cronSchedule": "*/5 * * * *"
  }
}
