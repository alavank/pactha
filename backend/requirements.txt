fastapi>=0.115.0
uvicorn[standard]>=0.34.0
# <2.1: o SQLAlchemy 2.1.0 (saiu em 24/09/2026 ~20h UTC) troca o driver padrao do
# "postgresql://" para o psycopg 3, que nao esta instalado — o CI quebrou em
# test_migrations_registradas e test_smoke_coleta. Migrar de driver e decisao a parte.
sqlalchemy[asyncio]>=2.0.0,<2.1
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
pyjwt[crypto]>=2.9.0
bcrypt>=4.2.0
pandas>=2.2.0
httpx>=0.28.0
curl_cffi>=0.7.0
anthropic>=0.50.0  # IA assistant (Claude API)
python-multipart>=0.0.9
playwright>=1.49.0
playwright-stealth>=2.0.0  # evasao anti-bot gov.br (import: playwright_stealth.Stealth)
cryptography>=43.0.0
slowapi>=0.1.9
beautifulsoup4>=4.12.0
lxml>=5.0  # parser do simec_par (o html.parser perdia o PNAE); vinha só de carona do python-docx
reportlab>=4.2.0
# leitura de PDF: o CRC do CAGEC so existe em PDF (ver ingestion/cagec_scraper.py)
pypdf>=5.1.0
python-docx>=1.1.0  # Geracao de Documentos (export DOCX)
openpyxl>=3.1.0  # leitura de .xlsx no acordofes_ingest (estava faltando)
pywebpush>=1.14.0  # Push web (VAPID) do Painel Executivo do prefeito
# Servidor MCP de leitura (mcp_app.py). ⚠️ 2.x renomeou FastMCP -> MCPServer e
# mudou o Context injetavel (mcp.server.mcpserver.context); travado em <3 para o
# proximo salto de major nao trocar a API por baixo.
mcp>=2.2.0,<3
