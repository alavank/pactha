{'confirmada': 70, 'parcial': 30}

[G-cagec-sismob-simec-crons (cético #1)] PARCIAL :: 1. cagec_scraper.py:1114-1122 — main() distingue erro/parcial/ok e conta degradadas (CRC ausente): única fonte com semântica de status compl
   CORRECAO: main() distingue erro/parcial/ok e conta `degradadas` (:1117-1122), e o default do lote é 11 (:382, não :370). Mas NÃO é a única fonte com status tripartite: sismob_obras.py:675-679 grava error/partial/success, e simec_termos, gconv_es, transfvol_go, transferegov_te, che_rs, convenios_rs também usam 'partial'. O tempo medido documentado no código é ~43-47s por município (não ~20s), logo 11 municípios ≈ 8 min (~480s), contra um kill interno que o próprio código diz ser de 25 min. O valor `timeout -k 30 1020` não aparece em nenhum lugar do repo (INFRA.md, CRON_SETUP.md, CONTINUAR.md) — só pode ser conferido no Coolify.
   NUANCE: Se o Coolify realmente tem `timeout -k 30 1020` (~17 min) e o código assume kill de 25 min, o comentário do código está desatualizado, mas 11×47s ≈ 8,6 min ainda cabe com folga ~2x. Os status são gravados como 'erro'/'parcial'/'ok' (em português), enquanto sismob usa 'error'/'partial'/'success' — vocabulário inconsistente entre fontes na mesma tabela ingestion_log. Além disso, :1124-1137 engole falha de INSERT no ingestion_log com apenas warning.
   EVID: backend/ingestion/cagec_scraper.py:1117-1122 `if falha and not ok: status='erro' / elif falha or degradadas: 'parcial' / else 'ok'`; :382 `_lote = max(0, int(os.getenv("CAGEC_LOTE_MUNICIPIOS", "11") or "11"))`; :371-379 

[G-cagec-sismob-simec-crons (cético #1)] PARCIAL :: 4. INFRA.md §5 afirma que simec-termos tem timeout -k 30 1700 e coluna timeout da task = interno+120 (1700); simec_termos.py:59 — SIMEC_TERM
   CORRECAO: O código e o INFRA.md batem entre si (1500 interno, `timeout -k 30 1700` no comando). Mas a regra de ouro do INFRA.md:226-227 exige que a COLUNA timeout da task seja ≥ N + 120 = 1820 s — não 1700. O texto de INFRA.md:314-315 confunde os dois números (diz que a coluna = interno+120 = 1700, quando 1700 é o N do `timeout -k 30`, e a coluna deveria ser ≥1820). Se a coluna medida no Coolify é 300 s, viola a regra de ouro em ~6x: o Coolify mata aos 5 min, antes do budget de 25 min, e descarta o stdout — exatamente o cenário de INFRA.md:243-246 (transferegov-te, 300→1720 em 17/08).
   NUANCE: Não posso verificar o valor 300 no Coolify (read-only no repo). Com a coluna em 300 s, a rodada de simec-termos nunca chega ao `partial` do ingestion_log que o comentário de simec_termos.py:55-56 promete ("Rodada cortada vira 'partial'"), pois o pcntl mata o processo Python de fora. Há precedente documentado do mesmo bug em transferegov-te (INFRA.md:243-246).
   EVID: INFRA.md:297-298 "`simec-termos`, `10 6 * * *`, com lock próprio ... e `timeout -k 30 1700`"; INFRA.md:314-315 "O orçamento interno é `SIMEC_TERMOS_BUDGET_S=1500` ... e a coluna `timeout` da task = interno + 120s (daí os

[G-cagec-sismob-simec-crons (cético #1)] PARCIAL :: 5. docs/CRON_SETUP.md:28-41 não lista watchdog, transferegov-lote, simec-termos, transferegov-te, private-keepalive, sismob nem cauc-manha (
   CORRECAO: A tabela de CRON_SETUP.md:28-40 (não :28-41) NÃO lista watchdog, transferegov-lote, simec-termos, transferegov-te, private-keepalive nem sismob — mas LISTA `cauc-manha` (:37). `simec-termos` e sismob aparecem só em prosa (:101-103, :109-111), não na tabela. A tabela tem só 3 colunas (Freitas/Trust/Monte Sião) — Santa Maria/RS não existe nela. A referência ao INFRA.md:208-211 está correta.
   NUANCE: Não posso confirmar quais tasks existem no Coolify da Freitas (fora do repo); INFRA.md:214-216 e :243-244 citam transferegov-lote e transferegov-te como existentes, e simec-termos em :297. CRON_SETUP.md:112-113 avisa explicitamente: 'confira as tasks reais no Coolify — elas são a fonte de verdade, não este documento'. A tabela de CRON_SETUP.md ainda mostra `transferegov` diário (0 2 * * *), enquanto INFRA.md:217-219 descreve o desenho novo (transferegov-lote em horas pares) — documento desatuali
   EVID: docs/CRON_SETUP.md:28-40 tabela com sigcon, transferegov, fns, govbr-renew, queue-sigcon, painel-alertas, cagec, cauc-manha (:37 `| cauc-manha | 25 10-14 * * * | ...`), gconv-es, transfvol-go, cofin-ses-go, tcm-go; INFRA

[G-cagec-sismob-simec-crons (cético #2)] PARCIAL :: cagec_scraper.py:1114-1122 — main() distingue erro/parcial/ok e conta `degradadas` (CRC ausente): única fonte com semântica de status comple
   CORRECAO: main() do CAGEC grava erro/parcial/ok (com `degradadas` = municípios sem CRC virando 'parcial'); CAGEC_LOTE_MUNICIPIOS default 11 (:382). NÃO é a única fonte com 3 estados: SISMOB (error/partial/success), FNS, simec_termos, transferegov_voluntarias e transfvol_go também distinguem parcial. O custo por município no código é 43-47s medidos (não 20s — os ~20s são de INFRA.md:325, texto de julho), logo 11 municípios ≈ 8 min, e o próprio comentário assume kill interno de 25 min; nenhum arquivo do repo cita `timeout -k 30 1020` (docs/CRON_SETUP.md:60-62 fala em 2700 interno / 3000 na coluna). O 1020 só pode vir do Coolify, não verificável aqui.
   NUANCE: O CAGEC usa dialeto 'ok/parcial/erro' e simec_termos.py:140-143 avisa que watchdog/freshness aceitam 'success' e 'ok' como sucesso mas 'partial'/'parcial' ficam de fora — então 'parcial' do CAGEC conta como não-sucesso para o watchdog. Se a task real tiver 1020s, uma rodada de 11 a 47s (~8-9 min) ainda cabe, mas um portal lento (INFRA.md cita 45 min máx para Freitas) estouraria.
   EVID: backend/ingestion/cagec_scraper.py:1117-1122 `if falha and not ok: status = "erro" / elif falha or degradadas: status = "parcial" / else: status = "ok"`; :382 `_lote = max(0, int(os.getenv("CAGEC_LOTE_MUNICIPIOS", "11") 

[G-cagec-sismob-simec-crons (cético #2)] PARCIAL :: INFRA.md §5 afirma que simec-termos tem `timeout -k 30 1700` e coluna timeout = interno+120 (1700); simec_termos.py:59 — SIMEC_TERMOS_BUDGET
   CORRECAO: O código e INFRA.md dizem exatamente isso (budget 1500, comando 1700). Porém a regra de ouro de INFRA.md:223-227 é: coluna `timeout` da task ≥ N + 120s onde N é o do `timeout -k 30 N` → exigiria coluna ≥ 1820 s, não 1700; INFRA.md:314-315 confunde as duas contas (aplica o +120 sobre o orçamento interno e chama de 'coluna', quando 1700 é o N do comando). Se a coluna real for 300 s, viola a regra por ampla margem (300 << 1820) — o Coolify mataria a task aos 5 min, antes do budget de 25 min, sem stdout e sem ingestion_log. O valor 300 no Coolify não é verificável neste repo.
   NUANCE: simec_termos commita por município (:136-138) e grava 'partial' quando corta pelo budget (:397), mas isso só ocorre se o processo chegar ao fim do budget interno; um kill pelo pcntl aos 300 s não deixa linha. O watchdog só aceita success/ok (simec_termos.py:140-143), então rodadas sempre cortadas pareceriam 'nenhum sucesso' — e :432-439 diz que o throttle interno conta 'partial' como rodada.
   EVID: INFRA.md:297-298 `simec-termos`, `10 6 * * *` ... `timeout -k 30 1700`; :314-315 `O orçamento interno é SIMEC_TERMOS_BUDGET_S=1500 ... a coluna timeout da task = interno + 120s (daí os 1700)`; :223-227 `E a coluna timeou

[G-cagec-sismob-simec-crons (cético #2)] PARCIAL :: docs/CRON_SETUP.md:28-41 não lista watchdog, transferegov-lote, simec-termos, transferegov-te, private-keepalive, sismob nem cauc-manha (que
   CORRECAO: CRON_SETUP.md:28-41 lista `cauc-manha` (linha 37) — a afirmação está errada nesse item. Não lista watchdog, transferegov-lote, transferegov-te, private-keepalive nem sismob na tabela; `simec-termos` aparece só em prosa (:101-111, com timeout 1700), não na tabela. A tabela só cobre 3 tenants (sem Santa Maria/RS) e o cabeçalho (:11-13) ainda fala em 'triplicata'. INFRA.md:208-211 declara a obsolescência e aponta o Coolify como fonte de verdade — confirmado.
   NUANCE: A existência dessas tasks no Coolify da Freitas não é verificável no repo; INFRA.md:215, :237, :262, :277 menciona transferegov-lote, transferegov-te, cauc-manha e watchdog como parte do desenho, o que corrobora que CRON_SETUP está atrasado. CRON_SETUP.md:101-103 ainda afirma que SISMOB 'não tem task própria de propósito' — contradiz a existência de uma task `sismob` se ela existir.
   EVID: docs/CRON_SETUP.md:28-41 tabela com sigcon, transferegov, fns, govbr-renew, queue-sigcon, painel-alertas, cagec, `cauc-manha` (:37), gconv-es, transfvol-go, cofin-ses-go, tcm-go; :110 menciona `simec-termos` em prosa; IN

[G-cagec-sismob-simec-crons (cético #2)] PARCIAL :: govbr_renew.py:58-70 — nota medida em 25/08/2026: 115 falhas de notas de empenho porque o keepalive tocava /execucao/ e não /prestacao/; tra
   CORRECAO: A nota das 115 falhas existe (govbr_renew.py:62-69), mas descreve o estado ANTERIOR: o código atual já navega PRESTACAO_ENTRY no keepalive (:282-293) e loga `prestacao=vivo/CAIU` separado (:304-308). A afirmação deve dizer: 'o keepalive tocava só /execucao/ até 25/08; HEAD já toca /prestacao/ também'. Projeto básico com SP frio em `logger.debug` (:795) — confirmado.
   NUANCE: tests/test_projeto_basico.py:79-81 cobre que a parede SAML retorna None (não {}), mas não cobre o nível de log. Com o nível INFO padrão dos crons, a mensagem 'recapturar sessao' nunca aparece no stdout do Coolify — o sintoma vira 'sem dado' silencioso, exatamente o que o comentário de :791-793 diz querer evitar.
   EVID: backend/ingestion/govbr_renew.py:62-69 `MEDIDO EM PRODUCAO (Freitas, 25/08/2026) ... notas de empenho ... 115 falhas (mora em /prestacao/ -> NUNCA tocado)`; :282-293 `# 4) prestacao (Notas de Empenho): SP SEPARADO ... aw

[C-log-watchdog (cético #2)] PARCIAL :: Estas 8 fontes gravam status "success" hardcoded, sem caminho de erro: acordofes_ingest.py:117-118, cauc_ingest.py:136-137, emendas_estaduai
   CORRECAO: Os 8 INSERTs gravam 'success' literal e nenhum deles tem um INSERT alternativo com 'failed'/'erro' — confirmado. Mas a frase 'exceção engolida' em siconv_emenda_backfill é imprecisa: a exceção É logada com logger.error (:218), não silenciada; o que acontece é que o erro não altera o status: a rodada é registrada como 'success' com records_inserted=0. Reescrever: 'em siconv_emenda_backfill.py:215-224, falha de backfill_ids (:212-214) ou backfill_parlamentar (:215-219) só vai para logger.error; o ingestion_log recebe success com n=0 em qualquer caso'.
   NUANCE: Nas 7 fontes restantes o 'caminho de erro' é a AUSÊNCIA de linha: se a coleta lança antes do INSERT (ex.: emendas_estaduais.py, cujo INSERT está fora de try dentro do `with conn`; siconv_convenio_backfill.py:131, sem try), nada é gravado — o watchdog só percebe pelo envelhecimento do último success (30h para siconv_convenio_backfill; as demais nem estão no catálogo, ver item 2). Também: simec_par.py:236 grava total_d+total_l como records_inserted mesmo que todos os municípios tenham dado `contin
   EVID: acordofes_ingest.py:117-118 `VALUES ('acordofes','success',%s,NOW())`; cauc_ingest.py:136-137 `('cauc','success',...)`; emendas_estaduais.py:287-289 `VALUES ('emendas_estaduais', 'success', :n, NOW())`; siconv_convenio_b

[E-opendata-pac-requirements] PARCIAL :: 2. transferegov_pac.py:460-463 e :523 — mesmo padrão de upsert engolido + success; NÃO existe guarda de completude equivalente no PAC; :216 
   CORRECAO: transferegov_pac.py:461-464 e :523-524 — mesmo padrão de upsert engolido + success; NÃO existe guarda de completude no PAC; :216 ORDER BY nome. O COLETOR grava TODAS as situações do PAC (Habilitada, Enviada para Análise, Cadastrada, Não Habilitada, Selecionada...); o filtro 'Selecionada/Selecionado' existe SOMENTE na leitura do RM (rm_builder.py:2166-2170). A tabela transferegov_pac contém o funil inteiro.
   NUANCE: (a) Mesmo problema do rollback em lote: :463 `conn.rollback()` com commit único em :465 descarta os upserts anteriores do município e `n` continua contando. (b) :527 `_registra_ingestao(total, ok=False, ...)` é chamado mesmo em dry-run (não tem `if not dry`), ao contrário do opendata. (c) Diferença relevante: o PAC usa COALESCE puro (:441-454), sem NULLIF — valor vazio nunca sobrescreve, mas também nunca limpa. (d) O PAC é chamado ao fim de run() do voluntarias (:2390 'PAC (apos voluntarias) fal
   EVID: backend/ingestion/transferegov_pac.py:461-464 `except Exception as e: logger.warning(f"upsert ..."); conn.rollback(); continue` | :523-524 `if not dry: _registra_ingestao(total, ok=True)` | :216 `SELECT id, nome, uf FROM

[E-opendata-pac-requirements] PARCIAL :: 6. requirements.txt — as 22 dependências usam >= sem teto (<); lxml não está listado e é importado em transferegov_http.py:30-31, chegando s
   CORRECAO: requirements.txt — as 24 dependências (não 22) usam >= sem teto; lxml não está listado e é importado em transferegov_http.py:30-31, chegando apenas como dependência transitiva de python-docx; Dockerfile.scraper:19 `playwright install --with-deps chromium` — a versão do Chromium é determinada pela versão de playwright resolvida no build (>=1.49.0, não pinada); Dockerfile.api não instala Chromium.
   NUANCE: Só o python-docx puxa lxml entre as 24 deps (verificado por pip show do python-docx; não verifiquei transitivas de todas as demais — 'só via python-docx' é plausível mas não exaustivo). transferegov_http.py é importado pelos scrapers do worker; a API também copia `backend/` inteiro (Dockerfile.api COPY backend/ ./), então um `pip install` que perca lxml quebraria em ambos. Não há lockfile (pip freeze) no repo, então cada build resolve versões novas — inclusive de playwright, e `playwright instal
   EVID: backend/requirements.txt: 24 linhas não-comentário, todas com `>=`, zero com `<` (contagem via grep) | grep lxml em requirements.txt: nada; transferegov_http.py:30-31 `from lxml import html as lxml_html` / `from lxml imp

[B-sessao-govbr] PARCIAL :: ingestion/govbr_keepalive.py:148-193 — _alert_session_dead e _clear_dead_alert (:196-206) não têm nenhum chamador no repo; e govbr_keepalive
   CORRECAO: _alert_session_dead e _clear_dead_alert não têm chamador algum (nem dentro do próprio cycle()); govbr_keepalive.py não é importado nem referenciado por nenhum Dockerfile, docker-compose, workflow ou script do repo (só citado em doc). Que a task Coolify `private-keepalive` execute `govbr_renew.py keepalive` NÃO é verificável no repo: o comando das Scheduled Tasks vive no Coolify; o repo só cita o nome da task (build-backend.yml:143) e o docker-compose local roda `govbr_renew.py` SEM argumento (modo renew).
   NUANCE: Impacto real: o único alerta Telegram de 'sessão gov.br expirou' do sistema é código morto — o keepalive que roda (govbr_renew.keepalive) só faz log.warning. Não há teste cobrindo. INFRA.md/CONTINUAR.md não mencionam 'keepalive' (grep vazio), então a afirmação sobre a task private-keepalive vem de conhecimento fora do repo.
   EVID: backend/ingestion/govbr_keepalive.py:148 `def _alert_session_dead():` / :196 `def _clear_dead_alert():` — grep no repo inteiro só acha as definições; cycle() (:209-238) devolve 'dead' sem chamar nenhuma das duas. Grep de

[B-sessao-govbr] PARCIAL :: ingestion/transferegov_http.py — não há nenhum sleep/backoff/tratamento de 429 ou Retry-After em todo o arquivo; ingestion/transferegov_te.p
   CORRECAO: transferegov_http.py não tem nenhum sleep, backoff, tratamento de 429 nem Retry-After. Porém transferegov_te.py NÃO é o único coletor com rate limiting: simec_termos (trata 403/429/503 + delay), transparencia_mg, sismob_obras, convenios_rs, consulta_popular_rs e transferegov_voluntarias (backoff exponencial no goto) também espaçam/recuam. Nenhum arquivo de ingestion/ lê o header Retry-After (grep vazio).
   NUANCE: O único que reage a 429 por status explícito é simec_termos.py:352; transferegov_te reage a 403 (com comentário :67-70 sobre 403 ≠ rate-limit no /opob). Nenhum trata Retry-After.
   EVID: transferegov_http.py: grep -i 'sleep|429|retry|backoff' → nenhuma ocorrência (nem `import time/asyncio`, :24-31). transferegov_te.py:77 `_PGTO_DELAY = float(os.getenv("TE_PGTO_DELAY", "0.5"))`, :88 `_PAGE_DELAY = ..."2"`

[A-transferegov-gates] PARCIAL :: ingestion/transferegov_voluntarias.py:672-678 — TG_SKIP_ENRICH=="1" pula todo o enrich e grava só linhas-base; a task Coolify diária `transf
   CORRECAO: A parte do código está certa: TG_SKIP_ENRICH=1 devolve só as linhas-base (mantendo id_proposta_siconv da URL) e o COALESCE do upsert preserva o detalhe anterior. TG_HTTP_DETALHE=1 significa: ler a página 'Dados da Proposta' via httpx (`_hx.detalhe`) em vez de navegar o Chromium — e SÓ tem efeito se `_hx is not None`, i.e. exige TG_HTTP_ENRICH=1 também. Já a afirmação sobre os envs das tasks Coolify (`transferegov` com TG_SKIP_ENRICH=1; `transferegov-lote` com TG_SKIP_ENRICH=0 TG_HTTP_DETALHE=1) NÃO é verificável no repo: o único comando documentado é o do cron legado em docs/CRON_SETUP.md:77 (TG_SKIP_ENRICH=1, sem TG_HTTP_*), e nem INFRA.md nem CONTINUAR.md listam envs das scheduled tasks (g
   NUANCE: Mesmo com TG_HTTP_DETALHE=1, se `_hx.detalhe` devolver None ou lançar, o código cai no browser (:822-826) — o detalhe base não se perde. Nota: no run() diário, o COALESCE também é o que evita que TG_SKIP_ENRICH=1 apague codigo_instrumento etc. (comentário :2112-2120).
   EVID: backend/ingestion/transferegov_voluntarias.py:672-679 — `if os.getenv("TG_SKIP_ENRICH") == "1": ... return propostas` (confirmado). :815 — `if _hx is not None and _idp and (os.getenv("TG_HTTP_DETALHE", "0") or "0").strip

[E-opendata-pac-requirements (cético #1)] PARCIAL :: ingestion/transferegov_pac.py:460-463 e :523 — mesmo padrão de upsert engolido + success; e NÃO existe guarda de completude equivalente no P
   CORRECAO: ingestion/transferegov_pac.py:461-464 e :524-525 — mesmo padrão de upsert engolido (com rollback que desfaz o lote não commitado) + status success; não existe guarda de completude equivalente à TG_GUARD_MIN_RATIO no PAC; :216 ORDER BY nome. O COLETOR GRAVA TODAS as situações do PAC (Habilitada, Enviada para Análise, Cadastrada, Não Habilitada, Selecionada...) em transferegov_pac; o filtro por "Selecionada"/"Selecionado" existe SOMENTE na leitura, em services/rm_builder.py:2161-2171.
   NUANCE: A parte 'só grava Selecionada' está errada — o coletor mapeia o enum (_SITUACAO :99-106) e persiste tudo; quem restringe é o RM. Mesmo defeito de rollback-em-lote da opendata (commit único em :465). Existe TG_PAC_DRY_RUN (:499). No except do run() PAC (:527-529) o _registra_ingestao(ok=False) é chamado mesmo em dry-run (diferente da opendata) — detalhe menor.
   EVID: backend/ingestion/transferegov_pac.py:461-464 `except Exception as e: logger.warning(f"upsert ..."); conn.rollback(); continue`; :524-525 `if not dry: _registra_ingestao(total, ok=True)`; :216 `SELECT id, nome, uf FROM m

[E-opendata-pac-requirements (cético #1)] PARCIAL :: backend/requirements.txt — as 22 dependências usam >= sem teto (<); lxml não está listado e é importado em transferegov_http.py:30-31, chega
   CORRECAO: backend/requirements.txt — as 24 dependências (não 22) usam >= sem teto (<); lxml não está listado e é importado em transferegov_http.py:30-31, chegando apenas como dependência transitiva de python-docx; Dockerfile.scraper:19 roda `playwright install --with-deps chromium`, cujo Chromium é a revisão casada com a versão do pacote `playwright` instalada — que não está pinada (`playwright>=1.49.0`); Dockerfile.api não instala Chromium.
   NUANCE: (1) Contagem errada: 24 pacotes, não 22. (2) 'chegando só via python-docx' foi verificado no ambiente local (pip show), não na imagem Docker; beautifulsoup4 e playwright não exigem lxml, então a conclusão se sustenta, mas se python-docx for removido do requirements o scraper HTTP do TransfereGov quebra no import. (3) `playwright install chromium` sempre baixa a revisão vinculada à versão do pacote Python — a 'falta de pin' está no requirements (`playwright>=1.49.0`), não no comando do Dockerfile
   EVID: backend/requirements.txt: `grep -c '^[a-zA-Z]'` → 24 linhas de dependência (fastapi ... pywebpush), todas com `>=`; `grep '<'` → nenhuma; grep -rn lxml → só backend/ingestion/transferegov_http.py:30-31 `from lxml import 

[B-sessao-govbr (cético #1)] PARCIAL :: ingestion/govbr_keepalive.py:148-193 — _alert_session_dead e _clear_dead_alert (:196-206) não têm nenhum chamador no repo; e govbr_keepalive
   CORRECAO: _alert_session_dead e _clear_dead_alert não têm chamador algum (nem dentro de govbr_keepalive.py); govbr_keepalive.py não é importado nem agendado por nenhum compose/workflow/doc do repo (só é executável a mão via __main__, :250-255). Que a task Coolify 'private-keepalive' executa `govbr_renew.py keepalive` NÃO é verificável no repo: o nome da task aparece só na denylist do build-backend.yml:143, e o comando vive na configuração do Coolify.
   NUANCE: O alerta Telegram de sessão morta (:180-183) nunca foi ligado: mesmo se alguém rodasse govbr_keepalive.py, `cycle()` retorna 'dead' sem alertar. O único keepalive agendado VISÍVEL no repo é o cron ofelia do docker-compose.yml:88 (`*/15` chamando renew(), não keepalive()), e docs/CRON_SETUP.md:33/86 também só citam govbr-renew.
   EVID: backend/ingestion/govbr_keepalive.py:148 `def _alert_session_dead():` e :196 `def _clear_dead_alert():`; grep no repo inteiro (excl. node_modules) só acha as definições — `cycle()` (:209-237) loga 'sessao MORTA/guest' (:

[B-sessao-govbr (cético #1)] PARCIAL :: routers/session_capture.py:232-249 — após capturar govbr/siconv_legado a API dispara transferegov_voluntarias.run() em background no process
   CORRECAO: A exceção NÃO é 'engolida' em :242-243 — é registrada com traceback via logger.exception no logger 'auto-scrape'. O que ocorre é: o pacote playwright está instalado na imagem da API (requirements.txt:15), então o import de :236 passa, create_task sucede, auto_scrape=True e a resposta já diz 'scraper iniciado' ANTES de o task rodar; o chromium.launch falha dentro de _bg_scrape (binário ausente) e vira apenas uma linha de log do container da API, sem refletir na resposta nem em scraper_jobs.
   NUANCE: Comentário do Dockerfile.api:2-3 diz que 'o gatilho on-demand apenas enfileira jobs na tabela scraper_jobs', contradizendo este caminho que chama run() direto no processo da API (uvicorn --workers 2, :44 do Dockerfile). Não verifiquei se playwright-stealth ou outro import em transferegov_voluntarias falha antes do launch — em qualquer caso a falha fica só no log do worker uvicorn.
   EVID: backend/routers/session_capture.py:233 `if payload.automation_key in ("govbr", "siconv_legado"):`, :236 `from ingestion.transferegov_voluntarias import run as _run_tg`, :245 `_aio.create_task(_bg_scrape())`, :246 `auto_s

[B-sessao-govbr (cético #1)] PARCIAL :: ingestion/transferegov_http.py — não há nenhum sleep/backoff/tratamento de 429 ou Retry-After em todo o arquivo; ingestion/transferegov_te.p
   CORRECAO: transferegov_http.py não tem sleep/backoff/429/Retry-After (confirmado). Mas transferegov_te.py NÃO é o único coletor com rate limiting: é o único com backoff escalonado específico para 403 de rate-limit (:89); simec_termos.py tem delay fixo entre requisições e reconhece 429; transferegov_voluntarias.py tem retry com backoff exponencial para erros transitórios de goto; vários outros têm sleeps fixos. Nenhum arquivo de ingestion lê Retry-After.
   NUANCE: transferegov_http.py é chamado de dentro de transferegov_voluntarias (enrich HTTP), que tem deadline/orçamento de tempo, mas isso limita duração e não frequência de requisições. Delays do TE são configuráveis por env (TE_PGTO_DELAY, TE_PAGE_DELAY).
   EVID: grep -i 'sleep|429|retry|backoff|too many' em backend/ingestion/transferegov_http.py: zero ocorrências; imports :25-31 sem time/asyncio; nenhum httpx.HTTPTransport(retries=). transferegov_te.py:77 `_PGTO_DELAY = float(os

[A-transferegov-gates (cético #2)] PARCIAL :: 4. TG_SKIP_ENRICH=="1" pula todo o enrich e grava só linhas-base; task Coolify diária `transferegov` roda com TG_SKIP_ENRICH=1 e `transfereg
   CORRECAO: TG_SKIP_ENRICH="1" pula todo o enrich e grava só linhas-base (:672-679). O comando documentado da task diária `transferegov` usa TG_SKIP_ENRICH=1 (docs/CRON_SETUP.md:77). As envs da task `transferegov-lote` (TG_SKIP_ENRICH=0, TG_HTTP_DETALHE=1) NÃO constam em nenhum arquivo do repo — INFRA.md:207-210 declara que a fonte de verdade das tasks é o Coolify; essa parte é não verificável aqui. TG_HTTP_DETALHE=1 (:815) faz o DETALHE da proposta ser lido por GET HTTP via `_hx.detalhe` em vez de navegar o Chromium, e só tem efeito se _hx não for None (TG_HTTP_ENRICH=1); None/erro cai no browser (:822-826).
   NUANCE: Ponto importante: TG_HTTP_DETALHE=1 sozinho é inócuo — o gate :815 exige `_hx is not None`, isto é, TG_HTTP_ENRICH=1. Se a task lote tem TG_HTTP_DETALHE=1 sem TG_HTTP_ENRICH=1 (como a afirmação sugere), o detalhe continua por browser. Nenhum doc do repo registra TG_HTTP_ENRICH nas tasks. O comentário de :2122-2128 confirma que o cron diário roda com TG_SKIP_ENRICH=1. docs/CRON_SETUP.md pode estar desatualizado (INFRA.md avisa que a tabela de horários envelheceu 2x).
   EVID: backend/ingestion/transferegov_voluntarias.py:672-679 — `if os.getenv("TG_SKIP_ENRICH") == "1": ... return propostas` (só guarda id_proposta_siconv). docs/CRON_SETUP.md:77 — `TG_SKIP_ENRICH=1 SIGCON_CONCURRENCY=1 flock -

[A-transferegov-gates (cético #2)] PARCIAL :: 5. UPSERT usa COALESCE(EXCLUDED.x, tabela.x) para situacao_contratacao_detalhe, projeto_basico, notas_empenho, historico_comunicacoes, docum
   CORRECAO: O UPSERT usa COALESCE para esses seis campos (:2138-2147): retorno None preserva o valor antigo sem marcador. Dois carimbos avançam, não um: historico_atualizado_em (só quando _hc is not None, :1029-1035 → :2186-2187) e detalhe_atualizado_em (sempre que o detalhe foi lido nesta rodada, `_detalhe_lido`, :830 → :2190) — este último avança mesmo em rodada guest, porque o detalhe é lido de qualquer forma (HTTP ou browser). Além disso `raw_data` e `updated_at` são sempre sobrescritos (:2155).
   NUANCE: Existe uma exceção ao 'nunca apaga': `_clausula_conferida_sem` (:865-877) apaga o JSONB de cláusula quando a tela devolve situação de contratação não-vazia e sem cláusula (rotina após o upsert, :2194+). Para notas_empenho o gate no Python é `is not None` (:2181): `[]` grava e sobrescreve; só None preserva. Para os demais (projeto_basico, historico, documentos, processo_execucao) o gate é truthy (:2171-2185), logo `{}`/`[]` também viram NULL e são preservados pelo COALESCE. Como detalhe_atualizad
   EVID: backend/ingestion/transferegov_voluntarias.py:2138-2147 — `situacao_contratacao_detalhe=COALESCE(...)`, `processo_execucao=COALESCE(...)`, `projeto_basico=COALESCE(...)`, `notas_empenho=COALESCE(...)`, `historico_comunic

[D-sigcon-fns-parsers (cético #2)] PARCIAL :: 4. sigcon_scraper.py:1074 — SIGCON_INDICACOES default "0": indicação, última alteração e prestação de contas (:1087) só são lidas com a env 
   CORRECAO: SIGCON_INDICACOES default "0": indicação, última alteração e prestação de contas só são lidas com a env =1 E enquanto o orçamento da rodada não estourou (a prestação de contas tem uma segunda checagem de orçamento própria em :1086). A parte 'a task Coolify sigcon da Freitas passa SIGCON_INDICACOES=1 inline' NÃO é verificável no repo — INFRA.md e CONTINUAR.md não mencionam SIGCON_INDICACOES (grep vazio; o único md que cita é schemas/convenio.py:58 como comentário).
   NUANCE: A afirmação omite a dupla dependência do orçamento: mesmo com a env =1, a partir do momento em que `_sig_estourou()` fica True nenhum plano subsequente recebe indicação/alteração/prestação de contas — e sem qualquer marcação de que aquele registro foi lido 'sem extras'. Com `_DET_MAX_PAG`/`SIGCON_MAX_PLANOS` e rodízio isso significa cobertura desigual e não observável desses campos. O que a Coolify passa como env está fora do repo e precisa ser checado no painel.
   EVID: ingestion/sigcon_scraper.py:1074 `if data and (os.getenv("SIGCON_INDICACOES", "0") or "0").strip() == "1" and not _sig_estourou():` | :1075 `_ind = await _scrape_indicacoes(page)` | :1078 `_alt = await _scrape_alteracoes

[D-sigcon-fns-parsers (cético #2)] PARCIAL :: 7. Clientes httpx SEM timeout explícito: che_rs.py:380, convenios_rs.py:370, gconv_es.py:324 e :360, run_fns_local.py:377 (e govbr_keepalive
   CORRECAO: Clientes httpx construídos sem timeout no construtor: che_rs.py:380, convenios_rs.py:370, gconv_es.py:324 e :360, run_fns_local.py:377. govbr_keepalive.py:187 NÃO entra na lista — o `httpx.post` passa `timeout=20` (:189). Além disso, em todos os cinco casos cada requisição individual passa timeout explícito, então na prática nenhuma chamada roda com o default de 5s do httpx nem sem limite: che_rs.py:76 `TIMEOUT = 30` usado em :161-162 `client.get(…, timeout=TIMEOUT)`; convenios_rs.py:170 `timeout=60` e :188 `timeout=300`; gconv_es.py:104 `timeout=60` e :127 `timeout=180`; run_fns_local.py:241/:260/:316 `timeout=30/20/30`.
   NUANCE: O achado é cosmético: httpx SEM timeout no construtor usa default de 5s (não infinito), e aqui todas as chamadas sobrescrevem por requisição. A verificação dos 'demais' foi feita por grep de `httpx.Client(|httpx.AsyncClient(|httpx.get(|httpx.post(` em ingestion/ e leitura dos construtores multi-linha (transparencia_mg.py:382-383 timeout=60.0, fns_scraper.py:95-102 timeout=60, govbr_keepalive.py:216-217 timeout=30, sigcon_ckan_backfill.py:76, transfvol_go.py:264, cofin_ses_go.py:199, sismob_obras
   EVID: che_rs.py:380 `with httpx.Client(follow_redirects=True) as client:` | convenios_rs.py:370 `with httpx.Client(follow_redirects=True) as client:` | gconv_es.py:324 `with httpx.Client() as client:` e :360 `with httpx.Client

[H-frontend-selos-cobertura (cético #1)] PARCIAL :: 2. routers/freshness.py:175-287 exige role admin (:180-181); _SUCESSO (:26), _DEGRADADO (:36); fresco ≤2d, atrasado ≤7 (:161-168). Não há te
   CORRECAO: ...GET /api/control/cobertura (routers/control.py:431-513) é o único endpoint que devolve a MATRIZ município×fonte completa (contagem de linhas por tabela). Porém o cliente não-admin vê cobertura parcial: GET /api/bi/overview devolve `frescor_carteira` (sigcon+transferegov, por carteira, com 'piores' nomeados) na visão consolidada, e as telas de convênios/emendas/CAUC mostram selo por município. Freshness é por FONTE (tenant-wide), não por município.
   NUANCE: O frontend tem /dashboard/configuracoes/frescor ('Status dos Dados', soAdmin em lib/configuracoes.ts:41) — é tela admin-only por fonte. A rota do freshness é /api/admin/freshness (main.py:219), sem 'AUTHZ_MODO' afrouxando: comentário :171-174 diz que a checagem de role vale nos dois modos.
   EVID: backend/routers/freshness.py:17 prefix=/api/admin/freshness; :175 `@router.get("", dependencies=[exige("frescor.ver")])`; :180-181 `if current.role != "admin": raise HTTPException(403...)`; :26 `_SUCESSO = ("success", "o

[H-frontend-selos-cobertura (cético #1)] PARCIAL :: 5. rm/page.tsx:699-714 — botões Completo/Resumido/Totalizado lado a lado; POST /rm (:307-322) envia anos=[] (todos), fontes=[] (todas), esta
   CORRECAO: rm/page.tsx:697-720 — a emissão é um dropdown 'Relatório' com Completo/Resumido em PDF e Word (Totalizado foi descontinuado: rm.py:889-891 devolve 400). O POST /rm (:307-322) envia a seleção do usuário (anosGerar/fontesGerar/estagioGerar), cujo DEFAULT é anos=[], fontes=[], estagio="" (= completo). rm_fontes.normalizar (:76-78) descarta chave desconhecida sem erro e reduz 'todas marcadas' a [].
   NUANCE: Chaves totalmente desconhecidas resultam em [] => RM COMPLETO silenciosamente (docstring :70-73), o que pode surpreender quem mandou filtro. O link 'Totalizado' do comentário :649 é lixo textual, não funcionalidade.
   EVID: frontend/src/app/dashboard/rm/page.tsx:697-720: dropdown 'Relatório' com 4 itens: 'Completo — PDF', 'Completo — Word', 'Resumido — PDF', 'Resumido — Word' — nenhum botão 'Totalizado' (só aparece num COMENTÁRIO na :649). 

[D-sigcon-fns-parsers] PARCIAL :: 1. sigcon_scraper.py:185-187 — tabela não carrega após a pesquisa → retorna [] com warning; _scrape_one segue, grava 0 convênios, chama _mar
   CORRECAO: sigcon_scraper.py:185-187 — quando a tabela não carrega em 90 s após a pesquisa, _scrape_municipio retorna [] com apenas um warning; _scrape_one (:1827) não distingue lista vazia de falha, ainda abre detalhes/emendas, grava 0 convênios, carimba _marca_coleta(ok=True) em :1911 e retorna True em :1918, contando em muns_ok (:2057) → status 'success' (:2081-2083) gravado em ingestion_log (:2093-2098).
   NUANCE: Linhas citadas estão deslocadas (+4/+7). Mecanismo confirmado. Nuance de impacto: o mesmo caminho vazio é INDISTINGUÍVEL de um município que legitimamente tem zero convênios (a wait_for_function rejeita a linha 'ui-datatable-empty-message', logo 'sem convênios' e 'não carregou' caem ambos no timeout de 90 s e no return []). Atenuante: o UPSERT não apaga registros antigos, então o efeito é 'dado congelado com carimbo fresco', não perda de dados. Não há flag 'lista vazia' em raw/ingestion_log; o r
   EVID: backend/ingestion/sigcon_scraper.py:185-187 `except Exception: logger.warning(f"  Tabela nao carregou apos pesquisa de {municipio_nome}"); return []` | :1827 `rows = await _scrape_municipio(page, _norm(nome))` (sem checa

[D-sigcon-fns-parsers] PARCIAL :: 4. sigcon_scraper.py:1074 — SIGCON_INDICACOES default "0": indicação, última alteração e prestação de contas (:1087) só são lidas com a env 
   CORRECAO: sigcon_scraper.py:1074 — SIGCON_INDICACOES default "0": indicação, última alteração e prestação de contas (:1087) só são lidas com a env =1 E enquanto o orçamento da rodada não estourou (a prestação de contas re-checa `_sig_estourou()` em :1086 e é a primeira a cair). Não há no repositório (INFRA.md, CONTINUAR.md, workflows) nenhum registro de que a task Coolify `sigcon` da Freitas passe SIGCON_INDICACOES=1 inline — essa parte fica sem verificação.
   NUANCE: A parte do código está correta. A parte sobre a task Coolify da Freitas é NÃO VERIFICÁVEL no repo: grep repo-wide por SIGCON_INDICACOES só encontra sigcon_scraper.py (:381, :579, :948, :1069, :1074) e schemas/convenio.py:58; INFRA.md não menciona a variável. Nuance de impacto: mesmo com a env ligada, a leitura extra é cortada pelo orçamento (`_sig_estourou`), então em rodadas apertadas os 3 campos podem faltar sem erro — cada helper é try/except→None.
   EVID: backend/ingestion/sigcon_scraper.py:1074 `if data and (os.getenv("SIGCON_INDICACOES", "0") or "0").strip() == "1" and not _sig_estourou():` | :1075-1080 `_scrape_indicacoes` / `_scrape_alteracoes` | :1086-1087 `if not _s

[D-sigcon-fns-parsers] PARCIAL :: 6. run_fns_local.py:199-201 — valores ausentes viram 0.0 (float(... or 0)), não NULL; :318 e :337 — HTTP≠200 ou exceção na paginação faz bre
   CORRECAO: run_fns_local.py:199-201 — valores ausentes viram 0.0 (float(... or 0)), não NULL; :318-320 e :335-337 — HTTP≠200 ou exceção na paginação faz break só do laço daquele ANO (o for por ano continua), e o município NÃO entra em `falhas` (único append em :387), gravando 'success' com anos faltando (:406-407); :377 — o httpx.AsyncClient é criado sem timeout no construtor, MAS todas as chamadas `client.get` (:236-241, :258-260, :306-316) passam timeout=30/20/30 explícito, então nenhuma requisição roda sem timeout.
   NUANCE: Linhas ligeiramente deslocadas (:318→:318-320, :337→:335-337, :388-391→:385-387). A parte do timeout é tecnicamente verdadeira mas sem impacto prático: o timeout por requisição cobre os 3 pontos de rede. Atenuante contra o 'success vazio': :401-404 grava 'error' quando total==0 na rodada inteira. O default do httpx (5 s) só valeria para chamadas sem kwarg, e não há nenhuma.
   EVID: backend/ingestion/run_fns_local.py:199-201 `vl_prop = float(p.get("vlProposta") or 0)` / `vl_pago = float(p.get("vlPago") or 0)` / `vl_pagar = float(p.get("vlPagar") or 0)` e :212 `"valor": vl_pago or vl_prop or 0` | :31

[D-sigcon-fns-parsers] PARCIAL :: 7. Clientes httpx SEM timeout explícito: che_rs.py:380, convenios_rs.py:370, gconv_es.py:324 e :360, run_fns_local.py:377 (e govbr_keepalive
   CORRECAO: Clientes httpx cujo CONSTRUTOR não define timeout: che_rs.py:380, convenios_rs.py:370, gconv_es.py:324 e :360, run_fns_local.py:377 — porém em todos eles cada requisição passa timeout explícito por chamada (che_rs 30 s; convenios_rs 60/300 s; gconv_es 60/180 s; run_fns_local 30/20/30 s), logo nenhuma requisição corre sem timeout. govbr_keepalive.py:187 (post do Telegram) TEM timeout=20 e não pertence à lista. Os demais construtores de cliente em ingestion/ definem timeout (grep confirma: acordofes 60/180, cauc 60/180, cofin 90, consulta_popular 60, fns_scraper 60, keepalive:216 30, scraper_base 30/120, siconv_* 600/900, sigcon_ckan 180, simec_termos 60, sismob TIMEOUT=45, tcm_go 120, transfe
   NUANCE: A afirmação sobre govbr_keepalive.py:187 é REFUTADA (timeout=20 está na própria chamada, :189). As demais são verdadeiras apenas no nível do construtor; o impacto real é nulo enquanto ninguém adicionar uma chamada nova sem kwarg — o único risco é de manutenção (default httpx = 5 s cairia numa chamada futura sem timeout, não 'sem timeout').
   EVID: che_rs.py:380 `with httpx.Client(follow_redirects=True) as client:` mas :161-162 `client.get(..., headers=UA, timeout=TIMEOUT)` (TIMEOUT=30 em :76) | convenios_rs.py:370 `with httpx.Client(follow_redirects=True) as clien

[H-frontend-selos-cobertura (cético #2)] PARCIAL :: 2. routers/freshness.py:175-287 exige role admin (:180-181); _SUCESSO=("success","ok") (:26), _DEGRADADO=("partial","parcial") (:36); fresco
   CORRECAO: routers/freshness.py: GET /api/admin/freshness exige a permissão `frescor.ver` E role admin (dupla barreira, :175-181); ... Não há tela para cliente não-admin com matriz município×fonte; o único endpoint que devolve município×fonte é GET /api/control/cobertura (X-Control-Token, escopo control:data:read). Porém o cliente não-admin VÊ frescor por município nas telas de Convênios/Emendas/CAUC (selo por fonte da tela) e, na visão consolidada do BI, o bloco _frescor_carteira — só não existe a matriz completa.
   NUANCE: A afirmação de 'único endpoint município×fonte' é verdadeira para matriz completa, mas GET /api/bi overview (via _frescor_carteira, bi.py:353, só quando `cons`) já expõe agregados por fonte (sigcon/transferegov) com lista 'piores' por município ao usuário comum. A rota freshness exige ALÉM do role admin a permissão `frescor.ver` via `exige(...)` — a afirmação omite isso. Frontend frescor/page.tsx:79-86 chama /admin/freshness e mostra 'Apenas administradores acessam esta tela.' ao 403.
   EVID: freshness.py:175 `@router.get("", dependencies=[exige("frescor.ver")])`; :180-181 `if current.role != "admin": raise HTTPException(403, ...)`; :26 `_SUCESSO = ("success", "ok")`; :36 `_DEGRADADO = ("partial", "parcial")`

[H-frontend-selos-cobertura (cético #2)] PARCIAL :: 5. rm/page.tsx:699-714 — botões Completo/Resumido/Totalizado lado a lado; payload POST /rm (:307-322) envia anos=[] (todos), fontes=[] (toda
   CORRECAO: rm/page.tsx:697-716 — um dropdown 'Relatório' com quatro entradas empilhadas: Completo (PDF/Word) e Resumido (PDF/Word); Totalizado foi retirado (backend rm.py:889-891 devolve 400 para tipo='totalizado'; só um comentário desatualizado em page.tsx:649 ainda cita 'Totalizado'). O payload de POST /rm (:307-322) envia o estado cru dos seletores — anos/fontes/estagio — cujo valor padrão é [] / [] / "" (= todos), mas o usuário pode marcar recortes; services/rm_fontes.py:62-78 (normalizar) descarta chave desconhecida sem erro e transforma 'todas marcadas' em [].
   NUANCE: `estagio` é normalizado em routers/rm.py:310-312 (valor fora de 'pagas'/'pendentes' vira ""), não em rm_fontes. A afirmação de 'sempre []' é imprecisa: o payload envia a seleção atual (anos podem ser recortados, fontes marcadas, estágio escolhido); [] só é o default. Linhas de normalizar são 62-78, não 74-78.
   EVID: rm/page.tsx:697-716: dropdown 'Relatório' com 4 itens `exportar(rm.id, "completo", "pdf")`, `("completo","docx")`, `("resumido","pdf")`, `("resumido","docx")` — NÃO há item Totalizado e são itens empilhados de um menu (`


===== NUANCES das confirmadas (para o relatorio) =====

[C-log-watchdog] setup_db.py:226-236 — ingestion_log tem exatamente 9 colunas, sem índice além da PK, e nenhuma migration a alt
   NUANCE: models/ingestion_log.py espelha as mesmas 9 colunas (id com index=True no ORM, mas isso só vale se create_all criar a tabela — setup_db.py usa SQL cru, e não há CREATE INDEX). O bloco '-- Indexes' logo abaixo (setup_db.py:238+) não cita ingestion_log. Queries do watchdog (`GROUP BY source`, `SELECT DISTINCT source`) e do /api/status (`ORDER BY id DESC LIMIT 25`) fazem seq scan em source/status.

[C-log-watchdog] watchdog_coleta.py — STATUS_SUCESSO=("success","ok") (:158); nenhuma query lê records_inserted/processed/updat
   NUANCE: O nome no código é FRESCOR_HORAS_NACIONAL + FRESCOR_HORAS_POR_UF (não 'FRESCOR_HORAS'), combinados em frescor_esperado() (:126-131) por UF do tenant. Também faltam do catálogo: emendas_estaduais, siconv_emenda_backfill, sigcon_ckan_backfill (todas gravam ingestion_log mas nunca são cobradas). A única detecção é idade do último success (:183-201) e 'tem linha mas nunca sucesso' (:207-218); 'success' com records_inserted=0 é aceito como saudável. S

[C-log-watchdog] 8 fontes gravam status 'success' hardcoded sem caminho de erro (acordofes:117-118, cauc:136-137, emendas_estad
   NUANCE: Nas 8 fontes o 'caminho de erro' é a ausência de linha: exceção antes do INSERT propaga (ex.: emendas_estaduais __main__ faz traceback+sys.exit(1), :298-303) e nada é gravado — o watchdog só notaria por envelhecimento do último success, nunca por uma linha 'erro'. siconv_emenda_backfill é o pior: backfill_ids e backfill_parlamentar podem AMBOS falhar e ainda assim sai 'success' com 0. simec_par.py:224-225 `if data is None: continue` — municípios 

[C-log-watchdog] cagec_scraper.py:1118-1122 grava ok/parcial/erro; transferegov_opendata.py:612 e transferegov_pac.py:478 grava
   NUANCE: sigcon_scraper tem um terceiro ponto de gravação: :1945-1948 grava 'success' com error_message 'sem credenciais SIGCON cadastradas' quando o cofre está vazio (decisão deliberada para não alarmar tenants sem SIGCON), e :2072-2076 grava 'success' também quando _tentados == 0. 'parcial' do CAGEC e 'partial' de TE NÃO contam como sucesso no watchdog (:150-158) — hoje, com CRC recusado, toda rodada CAGEC é 'parcial' e a fonte alarma como parada (compo

[C-log-watchdog] cauc_ingest.py:139-141 e acordofes_ingest.py:120-122 — INSERT em ingestion_log em try/except sem log; transfer
   NUANCE: O mesmo padrão silencioso aparece em siconv_federal_ingest.py:169-170 (rollback sem log), sigcon_ckan_backfill.py:218-219, siconv_emenda_backfill.py:226-227 (`pass`) e simec_par.py:238-239 (`pass`). Como nível padrão dos coletores é INFO (basicConfig INFO), o logger.debug nunca aparece em produção. Em contraste, cagec (:1137) e sigcon (:1951, :2101) usam logger.warning.

[C-log-watchdog] routers/control.py:555 — status de POST /api/control/te/lote vem do corpo sem validação (str(...)[:20]); main.
   NUANCE: te/lote NÃO é anônimo: exige `require_control_scope("control:data:write")` (control.py:517) — a falta de validação é sobre o valor do status (qualquer string até 20 chars, ex. 'ok'/'partial'/'xyz'), não sobre quem chama; e 'transferegov_te' não está no catálogo do watchdog, então o status gravado ali não afeta alarme nenhum. A abertura de /api/status/ingestao é deliberada e registrada no registro fail-closed (registro_rotas.py:230), com justifica

[F-rm-retencao (cético #1)] 1. _fed_retem: 'dead' nunca entra no completo; 'ativa' de ano anterior só entra se _vigencia_vencida(dt_fim); 
   NUANCE: É comportamento DECIDIDO pelo dono (docstring :337-344, 'opção B') e coberto por teste: tests/test_rm_completo.py:415 `assert _fed_retem(2021, 2026, _APROV, True, dt_fim=None) is False`. O comentário em :1503-1506 admite explicitamente que 'Cadastramento que nunca foi celebrado não tem vigência -> continua fora'. Vale só para 'todos os anos' (anos_sel vazio): com seleção [2025, 2026] o item de 2025 entra (:353-354). Não há log/aviso do descarte —

[F-rm-retencao (cético #1)] 2. _fns_retem: em 'todos os anos' sem seleção, proposta FNS pré-empenho de ano anterior sem pagamento cai fora
   NUANCE: A docstring (:398-404) diz que isso é pedido do dono ('proposta antiga NUNCA empenhada sai'). A regra B de vigência vencida do #317 não foi estendida ao FNS — mas FNS (proposta de fundo a fundo) raramente tem 'vigência' no sentido de convênio, então o impacto pode ser menor que no SIGCON.

[F-rm-retencao (cético #1)] 3. :1354-1360 query de convenios_estadual não filtra por fonte; `is_fns = "FNS" in fonte_db or "MS" in fonte_d
   NUANCE: Substring 'MS' casaria qualquer fonte contendo 'MS' em maiúsculas (ex.: 'SIMS', 'CMS', 'MST'); hoje não vi fonte estadual com isso, mas é frágil. :1364 também já computa `dt_fim` com a mesma expressão, usada no ramo anual (:1521).

[F-rm-retencao (cético #1)] 4. _recorte_parte1 mantém só partes com ordem==1 (ou título 'parte 1'/'bras') para PDF e DOCX; _destino_comple
   NUANCE: O comentário :210-212 pertence ao bloco de `_e_pendencia`/`_CONCLUIDO_RE`, e a própria docstring de `_recorte_parte1` (:283-285) avisa que `_e_pendencia` NÃO é chamado — é código morto com comentário obsoleto, não uma contradição dentro do caminho vivo. A docstring de gerar_resumido_pdf (:241) já declara 'RESUMIDO = o COMPLETO recortado nas DEMANDAS EM BRASILIA (Parte 1)'; o título recebe sufixo '— DEMANDAS EM BRASÍLIA' (:295). Ou seja: exclusão 

[F-rm-retencao (cético #1)] 5. :2275-2294 grupo/seção/parte vazios não entram (sem placeholder); rm_pdf.py:775-776 só escreve algo quando 
   NUANCE: SIMEC termos e PAC estão sob `if completo:` (:2094, :2143), TE não. Os comentários (:2093, :2142) chamam de 'best-effort: tenant sem a tabela segue sem'. Como o conteúdo vai congelado em rm_relatorios.conteudo, uma falha transitória de query numa dessas fontes gera um RM permanentemente sem aquela fonte, sem marca no documento.

[F-rm-retencao (cético #1)] 6. routers/rm.py:845 PDF gerado de rm_relatorios.conteudo (JSONB congelado); auto-popular (:698-761) e upsert 
   NUANCE: O UnboundLocalError com completo=False só acontece se o laço SIGCON chegar a :1554 sem nenhum `st` atribuído antes na função — como toda atribuição é condicional a `completo`, é real, mas inalcançável pelo router (sempre True). Não há teste de montar_conteudo com completo=False que eu tenha encontrado.

[F-rm-retencao (cético #1)] 7. fix_municipio_acentos.sql:4 UPDATE municipios SET nome='Araújos' WHERE ibge_code='3104502' (Arinos; Araújos
   NUANCE: O predicado exige `AND nome = 'Araujos'`, então em qualquer banco onde 3104502 esteja como 'Arinos' (correto) é no-op; só produziria dano se Arinos estivesse gravado como 'Araujos'. Efeito prático: o acento de Araújos (3103900) NUNCA é corrigido por esta migration — não verifiquei se os seeds gravam 'Araújos' já acentuado (rm_builder.py:1318 sugere que o banco tem a grafia acentuada).

[G-cagec-sismob-simec-crons (cético #1)] 2. sismob_obras.py:594-607 — guarda contra encolhimento >1/5 da carteira e itens sem proposta_id (SISMOB_ALLOW
   NUANCE: A guarda de 1/5 só dispara quando `sumiriam > 0` e o piso é `max(1, ja_tinha//5)` — para carteira pequena (<10 obras) 1 obra sumindo já passa livre; com ja_tinha=0 (primeira coleta) nada é bloqueado. A falha de detalhe deixa rastro: raw_data._pactha.origem='listagem' e `_empresas=None` (não mexe em sismob_empresas, :558-559), mas o UPSERT ainda sobrescreve os campos da listagem e a rodada sai como 'success' sem contar detalhes falhos. Auto-thrott

[G-cagec-sismob-simec-crons (cético #1)] 3. simec_par.py:169-171 — exceção por município devolve None e run() segue gravando success hardcoded (:236) e
   NUANCE: Pior que 'success hardcoded': se TODOS os municípios devolverem None, o log grava success com records_inserted=0 — nenhum contador de falhas existe em run(). O upsert() (:184-213) abre conexão própria e não tem try — uma exceção no INSERT derruba o run() inteiro, que é engolido pelo warning em run_dadosabertos_cron.py:53-54 sem ingestion_log. run_all() roda ANTES do sigcon (run_sigcon_cron.py:31-32) e desconta do SIGCON_BUDGET_SECONDS (:46-62), e

[G-cagec-sismob-simec-crons (cético #1)] 6. govbr_renew.py:58-70 — nota medida em 25/08/2026: 115 falhas de notas de empenho porque o keepalive tocava 
   NUANCE: A afirmação descreve o estado ANTES da correção: no HEAD atual o keepalive já navega PRESTACAO_ENTRY (govbr_renew.py:76-78 define, :288 `await page.goto(PRESTACAO_ENTRY, ...)`), então as 115 falhas são histórico documentado, não bug vivo. O `logger.debug` em :796 continua: em produção com nível INFO a mensagem 'recapturar sessao' some, e o chamador só devolve None (o COALESCE preserva o dado antigo, :783), ou seja, SP frio de projeto básico é sil

[G-cagec-sismob-simec-crons (cético #2)] sismob_obras.py:594-607 — guarda contra encolhimento >1/5 da carteira e itens sem proposta_id (SISMOB_ALLOW_SH
   NUANCE: O piso é `max(1, ja_tinha//5)`, ou seja com carteira ≤5 obras qualquer sumiço de 2+ já bloqueia. `_upsert_empresas` é pulado quando o detalhe falha (:555, `None = detalhe falhou; nao mexer`), então dados antigos de empresas ficam preservados, mas fotos/parcelas/atividade ficam sem atualização e a rodada vai a 'success' se nenhum município retornou erro. raw_data registra `origem: listagem`, o que permite auditoria a posteriori.

[G-cagec-sismob-simec-crons (cético #2)] simec_par.py:169-171 — exceção por município devolve None e run() segue gravando success hardcoded (:236) e en
   NUANCE: Só a exceção de rede (cffi.get) é capturada; erro em `parse_relatorio` ou em `upsert` (por município) propaga e derruba run() inteiro — que é engolido com warning em run_dadosabertos_cron.py:56-57, sem nenhuma linha em ingestion_log (nem success nem error). Ou seja: 0 municípios coletados com 100% de falha HTTP → 'success' com 0 registros; 1 município com HTML inesperado → nenhum registro no log. Não há teste cobrindo simec_par.

[F-rm-retencao] 1. _fed_retem: dead nunca entra no completo; ativa de ano anterior só entra se _vigencia_vencida(dt_fim); _vig
   NUANCE: Só vale em 'todos os anos' (anos_sel vazio): com seleção explícita contendo 2025 o item entra (:353-354). Não há log/contador de itens descartados — a exclusão é silenciosa. Chamada estadual em :1508-1511 passa dt_fim=c.dt_vigencia_atual or c.dt_vigencia_final; se ambos NULL, cai no None. Comentário :1504-1507 declara isso como intencional ('Cadastramento que nunca foi celebrado ... continua fora'). tests/test_rm_completo.py cobre _fed_retem mas 

[F-rm-retencao] 2. _fns_retem: em todos os anos sem seleção, FNS pré-empenho de ano anterior sem pagamento cai fora (:421-425)
   NUANCE: Mais amplo que o afirmado: também cai fora proposta antiga COM vl_pago>0 cujo sit_cls seja 'ativa' (ex.: 'Em análise'), porque :421 exige status paga/empenhada/vigente E vl_pago>0. Docstring :398-404 documenta o corte como decisão do dono ('proposta antiga NUNCA empenhada sai do relatório'), ou seja, a assimetria com o federal é parcialmente deliberada, não só esquecimento.

[F-rm-retencao] 3. Query de convenios_estadual não filtra por fonte; is_fns é substring ('FNS' ou 'MS'); :1508-1511 chama _fed
   NUANCE: Valores de fonte hoje gravados: 'FNS' (ingestion/fns_scraper.py:185, run_fns_local.py:214) e 'sigcon_emendas' (sigcon_scraper.py:1917); nenhum contém 'MS' por acaso, então o substring não causa falso positivo com as fontes atuais — mas qualquer fonte futura com 'MS' no nome (ex.: '*-MS' de Mato Grosso do Sul) seria tratada como FNS. Ausência de filtro por fonte significa que o ramo SIGCON (:1498+) é o 'else' de tudo que não é FNS, incluindo fonte

[F-rm-retencao] 4. _recorte_parte1 mantém só partes com ordem==1 (ou título 'parte 1'/'bras'); _destino_completo nunca devolve
   NUANCE: O comentário :209-212 está acima de _CONCLUIDO_RE/_e_pendencia, que _recorte_parte1 NÃO chama (docstring :283-285 admite). O título da Parte 2 estadual ('PARTE 2 - DEMANDAS DO MUNICÍPIO (Instrumentos Estaduais)', rm_builder.py:1214) não contém 'bras', então o fallback por título também não a resgata. Docstring de gerar_resumido_pdf :241-253 declara explicitamente 'SÓ da Parte 1' como pedido do dono — a contradição é do comentário antigo, não do c

[F-rm-retencao] 5. Grupo/seção/parte vazios não entram no conteúdo (sem placeholder); rm_pdf.py:775-776 só escreve algo quando
   NUANCE: Os blocos SIMEC termos e PAC estão sob `if completo:` (:2094, :2143), então só o completo os tenta. Comentários (:2093, :2142) chamam isso de 'best-effort: tenant sem a tabela segue sem X' — a intenção declarada é tolerar tabela ausente, mas o `except Exception` engole também erro de SQL/dados. O `meta.fontes` do PDF lista as consultas SELECIONADAS, não as que efetivamente retornaram — não serve como sinal de falha.

[F-rm-retencao] 6. PDF gerado de rm_relatorios.conteudo (JSONB congelado); auto-popular e o upsert re-montam; rm.py:337-339 e 
   NUANCE: Nenhum chamador em routers/services usa completo=False (grep); tests/test_rm_completo.py:142-151 testa só _fed_retem/_fns_retem com completo=False, não montar_conteudo, então o UnboundLocalError não é coberto nem disparado hoje. Docstring :1174 ainda anuncia 'completo=False (RM ANUAL — legado)' como suportado — está quebrado.

[F-rm-retencao] 7. fix_municipio_acentos.sql:4 usa ibge_code 3104502 (Arinos) para Araújos (3103900); registrada em startup.py
   NUANCE: Códigos IBGE (Araújos=3103900, Arinos=3104502) conferidos por conhecimento e pelo fixture de teste, não contra o IBGE online. Efeito real depende do que está gravado: se o tenant Freitas tem Araújos com ibge_code errado 3104502 E nome ASCII, o UPDATE acerta o nome mas perpetua o código errado; se tem 3103900, o nome fica 'Araujos' sem acento (a intenção da migration não se cumpre). Nenhum cenário corrompe Arinos, pois exige nome='Araujos'.

[C-log-watchdog (cético #2)] setup_db.py:226-236 — ingestion_log tem exatamente 9 colunas (id, source, started_at, finished_at, status, rec
   NUANCE: models/ingestion_log.py:9 declara `id = Column(Integer, primary_key=True, index=True)` — o index=True só reflete a PK, não cria índice extra. Sem índice em (source, finished_at)/(status), as queries do watchdog (watchdog_coleta.py:184-188, GROUP BY source com filtro em lower(status)) e de routers/freshness.py:191/202 fazem seq scan da tabela inteira; hoje irrelevante em volume, mas a tabela só cresce (não há purga em lugar nenhum).

[C-log-watchdog (cético #2)] ingestion/watchdog_coleta.py — STATUS_SUCESSO=("success","ok") (:158); nenhuma query do arquivo lê records_ins
   NUANCE: (1) Além de transferegov_te e siconv_federal, também ficam fora do catálogo: emendas_estaduais, siconv_emenda_backfill, sigcon_ckan_backfill, transferegov_opendata está (:50). (2) Existe um segundo ramo (:200-217) que alerta fonte esperada com linha no log mas sem nenhum sucesso — mas só para fontes do catálogo, logo as ausentes seguem invisíveis. (3) Como o ramo 'fonte sem NENHUMA linha fica de fora' (:196-199), uma fonte catalogada que nunca gr

[C-log-watchdog (cético #2)] cagec_scraper.py:1118-1122 grava ok/parcial/erro (português); transferegov_opendata.py:612 e transferegov_pac.
   NUANCE: Há um segundo INSERT em sigcon_scraper.py:1945 (rodada sem credencial) — também usa o vocabulário 'success'/'erro'? não verifiquei o status ali. Vocabulário misto no repo: 'success'/'partial'/'failed'/'ok'/'parcial'/'erro' — o watchdog só aceita 'success' e 'ok' (:158), logo 'parcial' e 'partial' NÃO renovam o relógio, de propósito (comentário :144-157). Em sigcon_scraper 'success' com _tentados==0 (:2073-2078) renova o relógio sem dado novo. tra

[C-log-watchdog (cético #2)] cauc_ingest.py:139-141 e acordofes_ingest.py:120-122 — o INSERT em ingestion_log está num try/except sem log; 
   NUANCE: Pior que os quatro citados: siconv_emenda_backfill.py:225-226 e simec_par.py:238-239 fazem `except Exception: pass` (nem rollback). Em contraste, cagec (:1136-1137), sigcon (:2100-2101), sismob, simec_termos, transferegov_te, che_rs, fpe_rs, convenios_rs, consulta_popular_rs, gconv_es usam logger.warning — o padrão 'bom' existe no repo, mas não foi aplicado nos coletores mais antigos. Como os coletores rodam em cron com logging padrão INFO, o log

[C-log-watchdog (cético #2)] routers/control.py:555 — o status de POST /api/control/te/lote vem do corpo sem validação (str(...)[:20]); mai
   NUANCE: (1) /te/lote NÃO é aberto: exige `require_control_scope("control:data:write")` (control.py:517), i.e. token de integração da Alavank; o risco é de status arbitrário (ex.: 'sucesso', 'ok ' com espaço, 'running') gravado por um cliente autorizado mas com typo — e como o watchdog filtra por lower(status) IN ('success','ok'), 'partial' enviado ali nunca renova o relógio de transferegov_te (que aliás não está no catálogo do watchdog, ver item 2). O de

[E-opendata-pac-requirements] 1. opendata.py:594-597 — exceção no UPSERT por proposta só loga warning e continua; :668 chama _registra_inges
   NUANCE: (a) `n` só incrementa no sucesso (:593), então `records_inserted` no ingestion_log reflete as perdas — mas o status é 'success' e ninguém compara. (b) A guarda é GLOBAL (count sobre todos os municipios ativos, :646-650): um município que despenca para zero passa despercebido se o total ainda estiver ≥70%. (c) TG_SKIP_GUARD=1 desliga; nenhum outro arquivo (.py/.md/.sh) referencia TG_GUARD_MIN_RATIO ou TG_SKIP_GUARD, ou seja, o default vale em prod

[E-opendata-pac-requirements] 3. opendata.py:25-31 — docstring registra a rodada de 3600s cobrindo ~10 de 41 municípios em ordem alfabética 
   NUANCE: A docstring descreve o problema do scraper de NAVEGADOR (transferegov_voluntarias) — o opendata em si não sofre desse corte (baixa CSV inteiro, sem paginação). O ORDER BY nome em :352 do opendata é inócuo para cobertura: `_coleta` filtra todos os municípios de uma passada só pelo IBGE (:361-425); a ordem só afeta a sequência do UPSERT. zfill(6) só se aplica quando `numero.isdigit()` (:131) — número com letra volta cru. _numero_proposta: NR_PROPOS

[E-opendata-pac-requirements] 4. voluntarias.py:2396-2418 — _proximos_municipios ordena por ultima_coleta_em ASC NULLS FIRST, nome (sem term
   NUANCE: O comentário em voluntarias.py:2522 ('deixa o backoff agir') sugere que o autor acreditava haver backoff no TransfereGov — não há. Efeito prático: carimbar sempre gera round-robin honesto (nenhum starva), mas um município que falha sempre volta à fila no mesmo ritmo dos saudáveis. Como o lote é limitado por TG_BUDGET_S e TG_LOTE_MUNICIPIOS, um município problemático consome uma vaga a cada ciclo de rodízio. Fallback: se a tabela não existir, cai 

[E-opendata-pac-requirements] 5. voluntarias.py:440 — _municipios_pacta usa ORDER BY nome; run() diário itera em :2325-2341 sem deadline (TG
   NUANCE: O run() diário roda PRIMEIRO o opendata + backfills (:2236-2261), então a camada base fica completa mesmo que o loop de navegador seja morto pelo timeout externo — o corte alfabético agora só afeta a fatia autenticada (histórico, quadro resumo). O __main__ (:2643-2660) escolhe run() sem argumento, `lote[:N]` para run_proximos, id para run_one; qual deles o cron chama está no Coolify (INFRA não lista o comando exato nas linhas conferidas). No run(

[B-sessao-govbr] ingestion/govbr_renew.py:318-321 — sem argumento executa renew(); com "keepalive" executa keepalive() (:232-31
   NUANCE: Linhas deslocadas (main em :326-329, keepalive :232-323). Detecção de 'frio' é só por URL (`idp/` ou `sso.acesso` na page.url) — o mesmo ponto cego de _sessao_caiu: um SP que devolve 200 com muro SAML sem redirecionar é reportado como 'vivo'. Também toca o guest ENTRY (4 alvos, não 3). O retorno é só 'alive'/'private_dead'; exec/prest frios não alteram o código de retorno, só o log.

[B-sessao-govbr] ingestion/transferegov_http.py:164-200 — _sessao_caiu detecta por URL (idp.transferegov / sso.acesso) e _sem_c
   NUANCE: _sem_contexto também detecta 'Proposta não encontrada' (não só SAML). Os chamadores (:273, :313, :321, :432, ... :1204, :1238) só devolvem None/False — o 'sessão caiu' não vira registro em scraper_jobs/ingestion_log neste módulo; quem grava (se grava) é transferegov_voluntarias. _sessao_caiu só olha a URL final após follow_redirects, o que a própria docstring de _sem_contexto (:192-196) admite como ponto cego.

[B-sessao-govbr] routers/session_capture.py:232-249 — após capturar govbr/siconv_legado a API dispara transferegov_voluntarias.
   NUANCE: Linha exata da exceção engolida é :241-243 (dentro de _bg_scrape, logada com .exception) — o `except` de :247-249 cobre só a falha ao AGENDAR (import/create_task). O pacote `playwright` está em requirements.txt:15, então o import passa e o create_task tem sucesso; a falha ('Executable doesn't exist') só ocorre ao lançar o chromium, já dentro da task, e vai para o log 'auto-scrape' — a resposta HTTP já saiu como auto_scrape_started=true. A task ta

[B-sessao-govbr] Nenhum arquivo de backend/ingestion/ usa ETag, If-None-Match, If-Modified-Since ou trata 304; o cache dos dump
   NUANCE: Linhas reais :289-293 (±2). O TTL é configurável por env TRANSFEREGOV_CACHE_HORAS (default 20h) e o diretório por SICONV_CACHE_DIR (:100), podendo ser bind mount compartilhado entre tenants (comentário :297-301) — então um tenant reaproveita o dump baixado por outro, o que reduz o impacto da ausência de validação condicional. Arquivo com < 1000 bytes é ignorado como cache.

[A-transferegov-gates] ingestion/transferegov_voluntarias.py:727 — TG_HTTP_ENRICH tem default "0"; quando "0", _hx=None.
   NUANCE: Quando TG_HTTP_ENRICH=1, `TgHttpEnrich.__init__` (transferegov_http.py:233-248) aceita cookies=None sem levantar exceção — `_hx` é criado MESMO sem sessão gov.br no cofre (`_load_govbr_cookies` devolve None em transferegov_voluntarias.py:404). Ou seja, `_hx` não é sinônimo de 'sessão viva'; é só 'cliente HTTP existe'. O fallback para browser só acontece se o import/construtor falhar (:732-734).

[A-transferegov-gates] ingestion/transferegov_voluntarias.py:922 — projeto_basico só é lido se `_idp and _hx and (...)`; não existe e
   NUANCE: O próprio comentário :911-913 admite: 'Só o caminho HTTP tem; quem chega aqui sem _hx segue sem o campo e o COALESCE do upsert preserva o que já havia'. Além do gate de _hx, o campo também depende da sessão SP SAML `execucao` viva — com `_hx` presente mas sessão fria, `projeto_basico` retorna None e só um logger.info (:933) registra. `transferegov_http.py:705-763` implementa `projeto_basico` sob a etiqueta '(guest)', conflitando com o comentário 

[A-transferegov-gates] ingestion/transferegov_voluntarias.py:947-966 — notas_empenho exige `_hx` E TG_NES=="1"; o fallback browser `_
   NUANCE: Há um terceiro portão: `prop["detalhe"].get("Código do Instrumento")` (só instrumentos celebrados). E o fallback browser recebe `page_auth`, que pode ser None (rodada guest) — nesse caso `_extrai_notas_empenho(None, ...)` executaria com page nula; não verifiquei se a função trata isso (docstring :1384ss não menciona). TG_NES default é "0" — tenant sem a env nunca coleta NEs, o que rm_builder.py:899/953 reconhece.

[A-transferegov-gates] ingestion/transferegov_voluntarias.py:2107-2155 — o UPSERT usa COALESCE(EXCLUDED.x, tabela.x) para situacao_co
   NUANCE: Há um marcador parcial: `detalhe_atualizado_em` avança sempre que o detalhe base foi lido (:830 `_detalhe_lido`, :2153), independente de os campos autenticados terem vindo — portanto a proposta pode ficar 'fresca' (e ser pulada pelo skip incremental de 18h, :764/:794-800) mesmo com projeto_basico/NEs/histórico não renovados. Para o histórico, `_captura_historico_comunicacoes` distingue {} (checado, sem eventos) de None (caiu no login) (:1188-1199

[A-transferegov-gates] ingestion/transferegov_voluntarias.py:2362-2381 e :2554-2578 — o status do ingestion_log deriva só de falhas p
   NUANCE: No lote (:2519-2532) existe um terceiro fator além de falha e paginação: município com `_SCRAPE_STATE["parcial"]` (orçamento esgotado) não conta em ok_n, mas também não entra em falhas_mun/subcoletas — logo uma rodada onde todos ficaram parciais grava 'success' (ok_n=0, falhas_mun vazio → cai no else). Sessão que morre NO MEIO da rodada (SAML no /private/) também é invisível ao status: `_captura_historico_comunicacoes` devolve None e só há logger

[E-opendata-pac-requirements (cético #1)] ingestion/transferegov_opendata.py:594-597 — exceção no UPSERT por proposta só loga warning e continua; :668 c
   NUANCE: Linhas reais: guarda em :636-657 (comentário) / :642-657 (código); registro success em :669. NUANCE GRAVE não dita na afirmação: o `conn.commit()` só acontece em :598, depois do laço; cada `conn.rollback()` em :596 desfaz TODOS os INSERTs anteriores daquele município que ainda não foram commitados, mas `n` já foi incrementado para eles. Ou seja, uma única exceção no meio do lote perde o município inteiro até aquele ponto e ainda assim `records_in

[E-opendata-pac-requirements (cético #1)] ingestion/transferegov_opendata.py:25-31 — docstring registra a rodada de 3600s cobrindo ~10 de 41 municípios 
   NUANCE: Deslocamentos: TG_IGNORA_SITUACOES está em :373-376 (não :368-372); _numero_proposta vai até :133. A docstring descreve o PROBLEMA do scraper de navegador em 07/2026; a própria opendata é a mitigação (um download de CSV, sem paginação). O ORDER BY nome em :352 é irrelevante para cobertura aqui porque _coleta lê o CSV inteiro de uma vez e o laço de gravação (:660) só ordena o log — não há corte por tempo neste módulo.

[E-opendata-pac-requirements (cético #1)] ingestion/transferegov_voluntarias.py:2396-2418 — _proximos_municipios ordena por ultima_coleta_em ASC NULLS F
   NUANCE: Reforço: o PR #159 (commit 693a01e) só tocou sigcon_scraper.py e diz explicitamente que 'carimbo no erro' era a 'mesma correcao ja aplicada ao rodizio do transferegov' — o BACKOFF (item 2 do PR) nunca chegou ao transferegov. Como _marca_coleta do TG carimba sempre, o round-robin honesto existe; o que falta é a punição progressiva de município que falha repetidamente (ele volta à fila na mesma cadência dos saudáveis). A coluna `tentativas` É incre

[E-opendata-pac-requirements (cético #1)] ingestion/transferegov_voluntarias.py:440 — _municipios_pacta usa ORDER BY nome; o run() diário itera em :2325
   NUANCE: Impacto depende de QUAL função o cron chama: a docstring de run_proximos (:2454-2467) diz que o cron `transferegov` (base, 1x/dia) faz open data + backfills e o cron `transferegov-lote` (horário, run_proximos com TG_LOTE_MUNICIPIOS/TG_BUDGET_S) faz o navegador em rodízio. Se o cron diário chamar run(), ele segue sem deadline e em ordem alfabética, protegido apenas pelo `timeout -k 30 N` externo do Coolify (INFRA.md:223-228), que mata sem ingestio

[B-sessao-govbr (cético #1)] ingestion/govbr_renew.py:318-321 — sem argumento executa renew(); com "keepalive" executa keepalive() (:232-31
   NUANCE: O critério de 'CAIU' é só a URL final conter 'idp/' ou 'sso.acesso' (:266, :275, :289). transferegov_http.py:792 avisa que o SP `execucao` frio devolve 200 SEM redirecionar — logo `execucao=vivo` no keepalive pode ser falso positivo (mesma classe de problema do muro SAML da afirmação 3). Também: exceção no goto deixa exec_ok/prest_ok=False, logo 'CAIU' também sai em timeout de rede, não só em sessão fria. Retorno 'private_dead' não dispara nenhum

[B-sessao-govbr (cético #1)] ingestion/transferegov_http.py:164-200 — _sessao_caiu detecta por URL (idp.transferegov / sso.acesso) e _sem_c
   NUANCE: _sessao_caiu depende de follow_redirects: com o cliente seguindo redirects a URL final é a do IdP; sem seguir, resp.url continua a original e a checagem é cega (chamadores em :313, :432 etc. também exigem status 200). Nenhuma das duas marca a sessão como morta em cofre_senhas/ingestion_log — o sinal fica só no retorno/None e no log, como a afirmação diz.

[B-sessao-govbr (cético #1)] Nenhum arquivo de backend/ingestion/ usa ETag, If-None-Match, If-Modified-Since ou trata 304; o cache dos dump
   NUANCE: Linhas reais do cache: 288-293 (não 287-291). O TTL é configurável (TRANSFEREGOV_CACHE_HORAS, default 20h) e o diretório pode ser compartilhado entre tenants via SICONV_CACHE_DIR (:100), o que já reduz downloads repetidos — mas continua sendo re-download integral quando expira, sem validação condicional.

[A-transferegov-gates (cético #2)] 1. TG_HTTP_ENRICH tem default "0"; quando "0", _hx=None.
   NUANCE: Mesmo com TG_HTTP_ENRICH=1, falha ao instanciar TgHttpEnrich (ex.: import/cookies) volta a _hx=None com apenas um warning. Nenhum doc no repo (INFRA.md, CONTINUAR.md, docs/) registra TG_HTTP_ENRICH=1 em produção — a fonte de verdade das envs das tasks é o Coolify (INFRA.md:207-210), não o repo; transferegov_http.py:9 confirma 'default off = browser'.

[A-transferegov-gates (cético #2)] 2. projeto_basico só é lido se `_idp and _hx and (...)`; não existe extrator de projeto básico por browser: co
   NUANCE: A única função browser que toca a URL 'projeto-basico/private/' é `_captura_historico_impl` (:1178-1193), que alimenta historico_comunicacoes/documentos_quadro_resumo — NÃO o campo projeto_basico (situação do Termo de Referência). Além disso o gate exige cláusula (regex na tela OU JSONB no banco via _com_clausula, :769), e o próprio `_hx.projeto_basico` depende do SP SAML `execucao` (transferegov_http.py:763-789) — pode devolver None mesmo com _h

[A-transferegov-gates (cético #2)] 3. notas_empenho exige `_hx` E TG_NES=="1"; o fallback browser `_extrai_notas_empenho` está DENTRO do bloco gu
   NUANCE: Há um terceiro portão: 'Código do Instrumento' no detalhe (só celebrados). O fallback browser recebe `page_auth`, que pode ser None em rodada guest — não há guarda; a exceção seria engolida pelo except :972 com warning. No upsert (:2180-2181) notas_empenho usa `is not None`, então `[]` medido sobrescreve; só None preserva. rm_builder.py:899/953 já trata 'TG_NES desligado' como caso esperado.

[A-transferegov-gates (cético #2)] 6. O status do ingestion_log deriva só de falhas por município e paginação incompleta; sessão gov.br morta NÃO
   NUANCE: No lote há um terceiro insumo: `_SCRAPE_STATE['parcial']` (orçamento esgotado) marca o município com `_marca_coleta(ok=False)` e não conta em ok_n (:2517-2528), mas também não entra em falhas_mun, logo não muda o status do ingestion_log. Não há nenhum registro estruturado de 'rodada guest' — nem em ingestion_log nem em scraper_municipio_coleta; só logs. O rastro indireto é `_hc None` mantendo a proposta em `_propostas_sem_historico` (backlog de h

[D-sigcon-fns-parsers (cético #2)] 1. sigcon_scraper.py:185-187 — tabela não carrega → retorna [] com warning; _scrape_one grava 0 convênios, cha
   NUANCE: O mesmo caminho `return []` cobre também o caso legítimo de município SEM nenhum plano (a espera exige tr sem classe ui-datatable-empty-message, então tabela vazia = timeout de 90s = []). O código não distingue 'portal não respondeu' de 'zero convênios'. Como o upsert nunca apaga, o dano é congelar dados antigos com carimbo de coleta fresca (scraper_municipio_coleta.ultima_coleta_em=now, tentativas=0), não perder dados. O ingestion_log grava reco

[D-sigcon-fns-parsers (cético #2)] 2. sigcon_scraper.py:248-250 — falha na paginação da listagem faz break e grava página parcial sem flag; :1857
   NUANCE: Há um terceiro corte silencioso na mesma função: o orçamento (`_sig_estourou()` em :194-197) e o teto `page_n > 50` (:246-247) também encerram a listagem parcial só com logger.info. Em todos os casos o resultado vai para o mesmo `_marca_coleta(ok=True)`. Nos detalhes, além do except externo, cada iteração que falha em :1104-1108 apenas incrementa `falhas_seguidas` — a ausência de detalhe de um plano específico não é registrada em lugar nenhum alé

[D-sigcon-fns-parsers (cético #2)] 3. sigcon_scraper.py:1489-1495 — seleção vem de cofre_senhas JOIN municipios sem filtro m.active; cauc_ingest.
   NUANCE: grep de 'active' nos quatro arquivos: a única ocorrência em SQL é sigcon_scraper.py:1351 (`_municipio_id_lookup`, WHERE active=true AND uf='MG'), que NÃO é usada na fila de credenciais — ou seja, o próprio arquivo aplica o filtro num helper e ignora no caminho principal. Em contraste, run_fns_local.py:76 e :85 filtram `WHERE active = true`. Impacto: um município desativado que ainda tenha credencial no Cofre continua consumindo login + orçamento 

[D-sigcon-fns-parsers (cético #2)] 5. ingestion/base.py — parse_date_br, parse_decimal_br, clean_string, extract_year não são importadas por nenh
   NUANCE: A varredura confirma 'sem log' para os parsers listados; NÃO conferi individualmente que todos devolvem None (alguns podem devolver 0/"" em vez de None — p.ex. sismob_obras._num/_dec e siconv_federal_ingest._money não foram lidos linha a linha). O ponto de impacto se mantém: um formato novo do portal vira NULL/0 silencioso, sem contador nem warning, em ~23 dos 24 parsers.

[D-sigcon-fns-parsers (cético #2)] 6. run_fns_local.py:199-201 — valores ausentes viram 0.0 (float(... or 0)); :318 e :337 — HTTP≠200 ou exceção 
   NUANCE: (a) Os valores 0.0 alimentam a derivação de situação em :201-204 (`Pago`/`Empenhado`/`Em analise`/`Pendente`) — um vlPago ausente vira 'Pendente' em vez de desconhecido. (b) O timeout ausente no AsyncClient NÃO tem impacto prático: TODAS as chamadas passam timeout por requisição — :241 e :260 (`timeout=30`, `timeout=20` em _fetch_individuais) e :316 (`timeout=30` na listagem). (c) Há um guarda parcial: :401-404 grava `error` se o total da rodada 

[H-frontend-selos-cobertura (cético #1)] 1. services/coleta.py:12-36 frescor_coleta — selo só recebe data quando tentativas==0; usado em convenios.py:4
   NUANCE: Nas duas rotas o frescor só é consultado se municipio_id foi passado (senão (None,0) — selo some). coleta.py:29-31: tabela ausente => (None,0) silencioso. Na tela de convênios o selo some quando o filtro é só FNS (:452). Emendas usa estado local `coleta` (page.tsx:83,119), não `data.coleta_falhas` — mesma semântica. O aviso '>26h' é só cor/title, não texto.

[H-frontend-selos-cobertura (cético #1)] 3. routers/control.py:328-366 — GET /api/control/ingestion devolve records_inserted = inserted+updated (:343/:
   NUANCE: Faixa real: :328-366 (decorator em :328, except em :364-366). O bloco `ultimo_por_fonte` (DISTINCT ON) mostra status da última rodada por fonte, mas sem mensagem de erro — o consumidor da Central só sabe 'error'/'partial', não o porquê. Tabela ausente devolve {log:[], error:...} sem 'ultimo_por_fonte'.

[H-frontend-selos-cobertura (cético #1)] 4. routers/bi.py:247-327 (_frescor_carteira) cobre só sigcon e transferegov com janela fixa 24h (:286-289) e n
   NUANCE: Só aparece na visão CONSOLIDADA (`cons`), nunca para município único; passa pelo cache do overview (:394-406). Denominador do sigcon é só municípios da UF_DA_FONTE_SIGCON (:266-270); best-effort: erro => None. Fontes estaduais de ES/GO/RS e CAGEC/CAUC não entram.

[H-frontend-selos-cobertura (cético #1)] 6. dou/page.tsx:217, TransfereGovPropostas.tsx:541 e transferegov-pac/page.tsx:301 — placeholders Araújos hard
   NUANCE: O fallback 'MG' do FNS NÃO é só cosmético: `estado` vai como parâmetro `uf` da consulta (fns/page.tsx:217 `{ municipio, ano: a, uf: estado }`) e entra na assinatura do auto-refresh (:274). Só dispara se o município selecionado não tiver `uf` (selMun nulo => municipio também vazio, então na prática a consulta não parte). Placeholders são só exemplos de texto; grep em frontend/src não achou outros 'Araújos'.

[D-sigcon-fns-parsers] 2. sigcon_scraper.py:248-250 — falha na paginação da listagem faz break e grava página parcial sem flag de pag
   NUANCE: Além da exceção em :248-250, outros dois cortes também encerram a listagem só com log: orçamento estourado (:194-197, `_sig_estourou()`) e o teto `page_n > 50` (:246-247). Nenhum grava flag. Nos detalhes, note-se que quando `_scrape_detalhes` lança, `_prox_pag` fica no valor inicial 1 (:1830) e ainda assim é gravado como sucesso em :1911, ou seja o rodízio de página recua para 1 silenciosamente. O COALESCE preserva os campos de detalhe antigos, e

[D-sigcon-fns-parsers] 3. sigcon_scraper.py:1489-1495 — seleção de municípios do SIGCON vem de cofre_senhas JOIN municipios sem filtr
   NUANCE: grep por 'active' nos 4 arquivos confirma: o único uso em sigcon_scraper é :1351 (`_municipio_id_lookup`, `WHERE active=true AND uf='MG'`), função distinta da seleção de credenciais. Contraste: run_fns_local.py:76 e :85 filtram `active = true`. Em cauc/acordofes/te o impacto é gravar linhas para município inativo (não gasta janela de scraping); no SIGCON um município inativo com credencial no Cofre consome login+orçamento da rodada.

[D-sigcon-fns-parsers] 5. ingestion/base.py — parse_date_br, parse_decimal_br, clean_string, extract_year não são importadas por nenh
   NUANCE: Amostra verificada de parsers locais silenciosos (return None em except sem log): sigcon_scraper._parse_money :83-91 e _parse_date :94-106; gconv_es._parse_valor :55-64 e _parse_data :67-78; simec_par._parse_money :49-57; cofin_ses_go._data :77-87; sismob_obras._dec :169-175; transferegov_pac._money :124-135; transferegov_opendata._money :223-233 (o próprio arquivo só avisa em _data/_modalidade, não em _money); tcm_go._data :84-91; convenios_rs._

[H-frontend-selos-cobertura (cético #2)] 1. services/coleta.py:12-36 (frescor_coleta) só data quando tentativas==0 (:34); usado em routers/convenios.py
   NUANCE: Em convênios o selo é omitido quando o filtro de fontes está só em FNS (page.tsx:452) e só aparece se municipioId estiver definido; no backend, sem municipio_id devolve (None, 0) — ou seja, sem município selecionado não há selo nem aviso. Tabela ausente também devolve (None,0) silenciosamente (coleta.py:29-31): tenant novo sem scraper_municipio_coleta não mostra nada, nem aviso. O 'primeira fonte com linha vence' significa que em emendas uma linh

[H-frontend-selos-cobertura (cético #2)] 3. routers/control.py:328-366 — GET /api/control/ingestion devolve records_inserted = inserted+updated e não e
   NUANCE: Endpoint devolve dois blocos: `log` (últimas 80 linhas) e `ultimo_por_fonte` (DISTINCT ON por source). Em erro de tabela devolve `{"log": [], "error": ...}` sem `ultimo_por_fonte`. O error_message da ingestão fica visível por outro caminho: GET /control/cobertura expõe `erro`/`erro_em`/`tentativas` por município×fonte (control.py:497-500), vindos de scraper_municipio_coleta, não do ingestion_log.

[H-frontend-selos-cobertura (cético #2)] 4. routers/bi.py:247-327 (_frescor_carteira) cobre só sigcon e transferegov com janela fixa de 24h (:286-289) 
   NUANCE: Só é calculado quando `cons` (visão consolidada) é verdadeiro (bi.py:353) — visão de município único não recebe o bloco. Denominador do sigcon usa UF_DA_FONTE_SIGCON (uma única UF, bi_abas): em tenants fora dessa UF `base['sigcon']` nem é criado. Falha de tabela devolve None silenciosamente. Linhas exatas do intervalo 24h estão em :288-291, não :286-289 (deslocamento pequeno).

[H-frontend-selos-cobertura (cético #2)] 6. dou/page.tsx:217, components/TransfereGovPropostas.tsx:541 e transferegov-pac/page.tsx:301 — placeholders '
   NUANCE: O fallback 'MG' do FNS não é só cosmético: `estado` vai como parâmetro `uf` na chamada à API (fns/page.tsx:217 `{ municipio, ano: a, uf: estado }`) e entra na assinatura do auto-refresh (:274). Só dispara quando o município selecionado não tem UF ou não há município selecionado — com município válido de RS/GO/ES a UF real é usada. Os placeholders são apenas texto de exemplo em inputs; nenhum filtro é pré-preenchido com 'Araújos'.
